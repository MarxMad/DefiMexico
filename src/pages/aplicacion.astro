---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Aplicar - DeFi México" description="Únete a la comunidad más exclusiva de fundadores crypto en México">
	<div class="application-page">
		<!-- Hero Section con Logo -->
		<section class="hero-section">
			<div class="hero-background"></div>
			<div class="container">
				<div class="hero-logo-section">
					<div class="hero-logo">
						
						<div class="hero-text">
							<h1 class="hero-title">Aplica a DeFi México</h1>
							<p class="hero-subtitle">Únete a la comunidad más exclusiva de fundadores crypto en México</p>
						</div>
					</div>
				</div>

				<div class="hero-content">
					<div class="hero-left">
						<h2 class="hero-heading">Transforma tu Startup Crypto</h2>
						<p class="hero-description">
							Recibe $450k en financiamiento, acceso exclusivo a nuestra red de mentores y la oportunidad de construir el futuro de las finanzas descentralizadas en México.
						</p>
						<div class="hero-features">
							<div class="feature-item">
								<div class="feature-icon">💰</div>
								<span>Financiamiento de $450k</span>
							</div>
							<div class="feature-item">
								<div class="feature-icon">🚀</div>
								<span>Aceleración intensiva</span>
							</div>
							<div class="feature-item">
								<div class="feature-icon">🤝</div>
								<span>Red de mentores</span>
							</div>
							<div class="feature-item">
								<div class="feature-icon">🌐</div>
								<span>Conecta con VCs</span>
							</div>
						</div>
					</div>
					<div class="hero-right">
						<div class="stats-grid">
							<div class="stat-item">
								<span class="stat-value">$450k</span>
								<span class="stat-label">Financiamiento</span>
							</div>
							<div class="stat-item">
								<span class="stat-value">2 semanas</span>
								<span class="stat-label">Respuesta</span>
							</div>
							<div class="stat-item">
								<span class="stat-value">$2.5B</span>
								<span class="stat-label">Valoración</span>
							</div>
							<div class="stat-item">
								<span class="stat-value">50+</span>
								<span class="stat-label">Startups</span>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Beneficios Section -->
		<section class="benefits-section">
			<div class="container">
				<div class="section-header">
					<h2 class="section-title">¿Por qué DeFi México?</h2>
					<p class="section-subtitle">Descubre los beneficios exclusivos de nuestro programa de aceleración</p>
				</div>
				
				<div class="benefits-grid">
					<div class="benefit-card">
						<div class="benefit-icon">💎</div>
						<h3 class="benefit-title">Financiamiento Directo</h3>
						<p class="benefit-description">
							Recibe $450k en financiamiento sin dilución excesiva. Nos enfocamos en el crecimiento, no en el control.
						</p>
					</div>
					
					<div class="benefit-card">
						<div class="benefit-icon">🎯</div>
						<h3 class="benefit-title">Mentoría Especializada</h3>
						<p class="benefit-description">
							Acceso directo a fundadores exitosos y expertos en crypto que han construido empresas de $100M+.
						</p>
					</div>
					
					<div class="benefit-card">
						<div class="benefit-icon">🌍</div>
						<h3 class="benefit-title">Red Global</h3>
						<p class="benefit-description">
							Conecta con VCs, partners estratégicos y la comunidad crypto más influyente de Latinoamérica.
						</p>
					</div>
					
					<div class="benefit-card">
						<div class="benefit-icon">⚡</div>
						<h3 class="benefit-title">Aceleración Intensiva</h3>
						<p class="benefit-description">
							Programa de 12 semanas diseñado para escalar tu startup de 0 a 100 en tiempo récord.
						</p>
					</div>
					
					<div class="benefit-card">
						<div class="benefit-icon">🔗</div>
						<h3 class="benefit-title">Ecosistema Integrado</h3>
						<p class="benefit-description">
							Acceso a infraestructura, herramientas y servicios premium para startups crypto.
						</p>
					</div>
					
					<div class="benefit-card">
						<div class="benefit-icon">📈</div>
						<h3 class="benefit-title">Crecimiento Garantizado</h3>
						<p class="benefit-description">
							Metodología probada que ha ayudado a startups a alcanzar valuaciones de $100M+.
						</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Proceso Section -->
		<section class="process-section">
			<div class="container">
				<div class="section-header">
					<h2 class="section-title">Proceso de Aplicación</h2>
					<p class="section-subtitle">Solo 3 pasos para transformar tu startup</p>
				</div>
				
				<div class="process-steps">
					<div class="process-step">
						<div class="step-number">1</div>
						<h3 class="step-title">Aplica</h3>
						<p class="step-description">
							Completa el formulario con información detallada sobre tu startup y equipo.
						</p>
					</div>
					
					<div class="process-step">
						<div class="step-number">2</div>
						<h3 class="step-title">Entrevista</h3>
						<p class="step-description">
							Conversa con nuestro equipo sobre tu visión y estrategia de crecimiento.
						</p>
					</div>
					
					<div class="process-step">
						<div class="step-number">3</div>
						<h3 class="step-title">Decisión</h3>
						<p class="step-description">
							Recibe respuesta en 2 semanas y comienza tu viaje con DeFi México.
						</p>
					</div>
				</div>
			</div>
		</section>

		<section class="form-section">
			<div class="container">
				<div class="section-header">
					<h2 class="section-title">Formulario de Aplicación</h2>
					<p class="section-subtitle">
						Completa el formulario a continuación para aplicar a nuestro programa de aceleración. 
						<span class="highlight">Solo tomamos 2 minutos en revisar tu aplicación.</span>
					</p>
				</div>
				
				<div class="form-container">
					<div class="form-header">
						<div class="form-progress">
							<div class="progress-bar">
								<div class="progress-fill"></div>
							</div>
							<span class="progress-text">Paso 1 de 1</span>
						</div>
					</div>
					
					<form class="application-form">
						<div class="form-group">
							<label for="company-name">Nombre de la Empresa</label>
							<input type="text" id="company-name" name="companyName" required>
						</div>
						
						<div class="form-row">
							<div class="form-group">
								<label for="founder-name">Nombre del Fundador</label>
								<input type="text" id="founder-name" name="founderName" required>
							</div>
							<div class="form-group">
								<label for="founder-email">Email</label>
								<input type="email" id="founder-email" name="founderEmail" required>
							</div>
						</div>
						
						<div class="form-group">
							<label for="company-description">Descripción de la Empresa</label>
							<textarea id="company-description" name="companyDescription" rows="4" required></textarea>
						</div>
						
						<div class="form-row">
							<div class="form-group">
								<label for="category">Categoría</label>
								<select id="category" name="category" required>
									<option value="">Selecciona una categoría</option>
									<option value="defi">DeFi</option>
									<option value="infrastructure">Infraestructura</option>
									<option value="consumer">Consumer</option>
									<option value="gaming">Gaming</option>
									<option value="payments">Payments</option>
									<option value="privacy">Privacy</option>
								</select>
							</div>
							<div class="form-group">
								<label for="stage">Etapa</label>
								<select id="stage" name="stage" required>
									<option value="">Selecciona la etapa</option>
									<option value="idea">Idea</option>
									<option value="mvp">MVP</option>
									<option value="early-users">Usuarios Tempranos</option>
									<option value="product-market-fit">Product-Market Fit</option>
									<option value="scaling">Escalando</option>
								</select>
							</div>
						</div>
						
						<div class="form-group">
							<label for="problem">¿Qué problema resuelves?</label>
							<textarea id="problem" name="problem" rows="3" required></textarea>
						</div>
						
						<div class="form-group">
							<label for="solution">¿Cuál es tu solución?</label>
							<textarea id="solution" name="solution" rows="3" required></textarea>
						</div>
						
						<div class="form-group">
							<label for="market">Tamaño del mercado</label>
							<textarea id="market" name="market" rows="2" required></textarea>
						</div>
						
						<div class="form-group">
							<label for="team">Información del equipo</label>
							<textarea id="team" name="team" rows="3" required></textarea>
						</div>
						
						<div class="form-group">
							<label for="funding">Financiamiento actual</label>
							<input type="text" id="funding" name="funding" placeholder="Ej: $0, $50k, $500k+">
						</div>
						
						<div class="form-group">
							<label for="website">Website (opcional)</label>
							<input type="url" id="website" name="website" placeholder="https://tuempresa.com">
						</div>
						
						<div class="form-group">
							<label for="pitch-deck">Link al Pitch Deck (opcional)</label>
							<input type="url" id="pitch-deck" name="pitchDeck" placeholder="https://drive.google.com/...">
						</div>
						
						<div class="form-actions">
							<button type="submit" class="submit-button">
								<span class="button-text">Enviar Aplicación</span>
								<span class="button-icon">🚀</span>
							</button>
							<p class="form-note">
								* Recibirás confirmación por email en las próximas 24 horas
							</p>
						</div>
					</form>
				</div>
			</div>
		</section>
	</div>
</MainLayout>

<style>
	.application-page {
		padding-top: 0;
		margin-left: 260px;
		transition: margin-left 0.3s ease;
	}

	/* Cuando el sidebar está colapsado */
	.sidebar-nested.collapsed ~ .application-page {
		margin-left: 70px;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	/* Hero Section */
	.hero-section {
		padding: 6rem 0;
		background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
		position: relative;
		overflow: hidden;
	}

	.hero-background {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: 
			radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 80% 20%, rgba(139, 92, 246, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 40% 40%, rgba(16, 185, 129, 0.05) 0%, transparent 50%);
		animation: backgroundShift 20s ease-in-out infinite;
	}

	@keyframes backgroundShift {
		0%, 100% { transform: scale(1) rotate(0deg); }
		50% { transform: scale(1.1) rotate(1deg); }
	}

	/* Logo Section */
	.hero-logo-section {
		text-align: center;
		margin-bottom: 4rem;
	}

	.hero-logo {
		display: inline-flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.logo-image-container {
		position: relative;
		width: 120px;
		height: 120px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.hero-logo-image {
		width: 100%;
		height: 100%;
		object-fit: contain;
		border-radius: 20px;
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		filter: drop-shadow(0 10px 20px rgba(96, 165, 250, 0.2));
	}

	.hero-logo-image:hover {
		transform: translateY(-5px) scale(1.02);
		box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
		filter: drop-shadow(0 15px 30px rgba(96, 165, 250, 0.3));
	}

	.hero-text {
		text-align: center;
	}

	.hero-subtitle {
		font-size: 1.2rem;
		color: rgba(255, 255, 255, 0.8);
		margin: 1rem 0 0 0;
		font-weight: 500;
	}

	/* Hero Content */
	.hero-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		background: rgba(255, 255, 255, 0.02);
		padding: 3rem;
		border-radius: 20px;
		border: 1px solid rgba(255, 255, 255, 0.05);
		backdrop-filter: blur(20px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
	}

	.hero-heading {
		font-size: 2.5rem;
		font-weight: 700;
		color: white;
		margin: 0 0 1.5rem 0;
		background: linear-gradient(135deg, #60a5fa 0%, #a855f7 50%, #10b981 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.hero-features {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
		margin-top: 2rem;
	}

	.feature-item {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		color: rgba(255, 255, 255, 0.9);
		font-size: 0.9rem;
	}

	.feature-icon {
		font-size: 1.2rem;
	}

	.hero-title {
		font-size: clamp(2.5rem, 5vw, 4rem);
		font-weight: 700;
		color: white;
		margin: 0 0 2rem 0;
		letter-spacing: -0.02em;
	}

	.hero-description {
		font-size: 1.25rem;
		color: rgba(255, 255, 255, 0.8);
		max-width: 600px;
		margin: 0 auto 4rem auto;
		line-height: 1.6;
	}

	.stats-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 2rem;
		max-width: 600px;
		margin: 0 auto;
	}

	.stat-item {
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 16px;
		padding: 2rem;
		text-align: center;
	}

	.stat-value {
		display: block;
		font-size: 2.5rem;
		font-weight: 700;
		color: white;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 0.5rem;
	}

	.stat-label {
		font-size: 0.9rem;
		color: rgba(255, 255, 255, 0.6);
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	/* Section Headers */
	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-title {
		font-size: 3rem;
		font-weight: 700;
		color: white;
		margin: 0 0 1rem 0;
		background: linear-gradient(135deg, #60a5fa 0%, #a855f7 50%, #10b981 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.section-subtitle {
		font-size: 1.2rem;
		color: rgba(255, 255, 255, 0.8);
		max-width: 600px;
		margin: 0 auto;
		line-height: 1.6;
	}

	.highlight {
		color: #10b981;
		font-weight: 600;
	}

	/* Benefits Section */
	.benefits-section {
		padding: 6rem 0;
		background: #121212;
	}

	.benefits-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 2rem;
	}

	.benefit-card {
		background: rgba(255, 255, 255, 0.02);
		border: 1px solid rgba(255, 255, 255, 0.05);
		border-radius: 16px;
		padding: 2.5rem;
		transition: all 0.3s ease;
		backdrop-filter: blur(20px);
	}

	.benefit-card:hover {
		transform: translateY(-5px);
		border-color: rgba(96, 165, 250, 0.3);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
	}

	.benefit-icon {
		font-size: 3rem;
		margin-bottom: 1.5rem;
	}

	.benefit-title {
		font-size: 1.5rem;
		font-weight: 600;
		color: white;
		margin: 0 0 1rem 0;
	}

	.benefit-description {
		color: rgba(255, 255, 255, 0.7);
		line-height: 1.6;
		margin: 0;
	}

	/* Process Section */
	.process-section {
		padding: 6rem 0;
		background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
	}

	.process-steps {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 3rem;
		max-width: 1000px;
		margin: 0 auto;
	}

	.process-step {
		text-align: center;
		position: relative;
	}

	.step-number {
		width: 80px;
		height: 80px;
		background: linear-gradient(135deg, #10b981 0%, #059669 100%);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 2rem;
		font-weight: 700;
		color: white;
		margin: 0 auto 2rem auto;
		box-shadow: 0 10px 30px rgba(16, 185, 129, 0.3);
	}

	.step-title {
		font-size: 1.5rem;
		font-weight: 600;
		color: white;
		margin: 0 0 1rem 0;
	}

	.step-description {
		color: rgba(255, 255, 255, 0.7);
		line-height: 1.6;
		margin: 0;
	}

	/* Form Section */
	.form-section {
		padding: 6rem 0;
		background: #121212;
	}

	.form-container {
		max-width: 800px;
		margin: 0 auto;
		background: rgba(255, 255, 255, 0.02);
		border: 1px solid rgba(255, 255, 255, 0.05);
		border-radius: 20px;
		padding: 3rem;
		backdrop-filter: blur(20px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
	}

	.form-header {
		margin-bottom: 2rem;
	}

	.form-progress {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin-bottom: 1rem;
	}

	.progress-bar {
		flex: 1;
		height: 6px;
		background: rgba(255, 255, 255, 0.1);
		border-radius: 3px;
		overflow: hidden;
	}

	.progress-fill {
		height: 100%;
		background: linear-gradient(135deg, #10b981 0%, #059669 100%);
		width: 100%;
		border-radius: 3px;
		transition: width 0.3s ease;
	}

	.progress-text {
		font-size: 0.9rem;
		color: rgba(255, 255, 255, 0.6);
		font-weight: 500;
	}

	.application-form {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.5rem;
	}

	.form-group {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.form-group label {
		font-size: 0.9rem;
		font-weight: 600;
		color: white;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		padding: 1rem;
		background: rgba(255, 255, 255, 0.1);
		border: 1px solid rgba(255, 255, 255, 0.2);
		border-radius: 8px;
		color: white;
		font-size: 1rem;
		transition: all 0.3s ease;
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: #3245ff;
		background: rgba(255, 255, 255, 0.15);
	}

	.form-group input::placeholder,
	.form-group textarea::placeholder {
		color: rgba(255, 255, 255, 0.5);
	}

	.form-group textarea {
		resize: vertical;
		min-height: 100px;
	}

	.form-actions {
		margin-top: 2rem;
		text-align: center;
	}

	.submit-button {
		padding: 1.2rem 3rem;
		background: linear-gradient(135deg, #10b981 0%, #059669 50%, #047857 100%);
		color: white;
		border: none;
		border-radius: 12px;
		font-size: 1.1rem;
		font-weight: 700;
		cursor: pointer;
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		box-shadow: 0 8px 25px rgba(16, 185, 129, 0.25), 0 4px 10px rgba(0, 0, 0, 0.1);
		backdrop-filter: blur(20px);
		border: 2px solid transparent;
		position: relative;
		overflow: hidden;
		display: flex;
		align-items: center;
		gap: 0.5rem;
		justify-content: center;
	}

	.submit-button::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
		transition: left 0.5s;
	}

	.submit-button:hover::before {
		left: 100%;
	}

	.submit-button:hover {
		background: linear-gradient(135deg, #059669 0%, #047857 50%, #065f46 100%);
		transform: translateY(-3px) scale(1.02);
		box-shadow: 0 12px 35px rgba(16, 185, 129, 0.35), 0 6px 15px rgba(0, 0, 0, 0.15);
		border-color: rgba(255, 255, 255, 0.3);
	}

	.submit-button:active {
		transform: translateY(-1px) scale(0.98);
		transition: all 0.1s;
	}

	.button-icon {
		font-size: 1.2rem;
	}

	.form-note {
		font-size: 0.9rem;
		color: rgba(255, 255, 255, 0.6);
		text-align: center;
		margin-top: 1rem;
	}

	/* Móviles */
	@media (max-width: 900px) {
		.application-page {
			margin-left: 0;
			padding-top: 80px;
		}
		
		.container {
			padding: 0 1rem;
		}
		
		.hero-content {
			grid-template-columns: 1fr;
			gap: 2rem;
			padding: 2rem;
		}
		
		.hero-heading {
			font-size: 2rem;
		}
		
		.hero-features {
			grid-template-columns: 1fr;
		}
		
		.logo-image-container {
			width: 100px;
			height: 100px;
		}
		
		.benefits-grid {
			grid-template-columns: 1fr;
		}
		
		.process-steps {
			grid-template-columns: 1fr;
			gap: 2rem;
		}
		
		.form-container {
			padding: 1.5rem;
			margin: 1rem;
		}
		
		.form-row {
			grid-template-columns: 1fr;
			gap: 1rem;
		}
		
		.stats-grid {
			grid-template-columns: 1fr 1fr;
			gap: 1rem;
		}
		
		.hero-title {
			font-size: 2.2rem;
			line-height: 1.2;
		}
		
		.section-title {
			font-size: 2rem;
		}
		
		.hero-description {
			font-size: 1.1rem;
		}
		
		.form-group input,
		.form-group select,
		.form-group textarea {
			padding: 0.8rem;
			font-size: 1rem;
		}
		
		.submit-button {
			padding: 0.8rem 2rem;
			font-size: 1rem;
			width: 100%;
		}
	}

	/* Tablets */
	@media (min-width: 901px) and (max-width: 1200px) {
		.container {
			padding: 0 2rem;
		}
		
		.form-container {
			padding: 2.5rem;
		}
		
		.hero-title {
			font-size: 3rem;
		}
	}

	/* Desktop */
	@media (min-width: 1201px) {
		.container {
			padding: 0 3rem;
		}
		
		.form-container {
			padding: 3rem;
		}
		
		.hero-title {
			font-size: 3.5rem;
		}
	}
</style>

<script>
	document.addEventListener('DOMContentLoaded', function() {
		const form = document.querySelector('.application-form');
		
		if (form) {
			form.addEventListener('submit', function(e) {
				e.preventDefault();
				
				// Aquí puedes agregar la lógica para enviar el formulario
				alert('¡Gracias por tu aplicación! Te contactaremos pronto.');
				
				// Opcional: enviar a un endpoint
				// const formData = new FormData(form);
				// fetch('/api/apply', {
				//   method: 'POST',
				//   body: formData
				// });
			});
		}
	});
</script> 