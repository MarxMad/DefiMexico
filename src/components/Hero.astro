git---
// Hero section component for DeFi México
---

<section id="hero" class="hero">
  <div class="hero-content">
    <img src="/DEFI.jpg" alt="Logo DeFi México" class="hero-logo-img" />
    <h1 class="hero-title">
      Ayudamos a startups
      <br />
      de DeFi a alcanzar
      <br />
      la velocidad de escape.
    </h1>
    
    <p class="hero-subtitle">
      DeFi México es el acelerador líder y comunidad de fundadores en el ecosistema crypto de México.
    </p>
    
    <div class="hero-stats">
      <div class="stat-item">
        <span class="stat-label">Valoración Combinada</span>
        <span class="stat-value">$2.5B</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 100px 2rem 0 2rem;
    background: linear-gradient(135deg, #121212 0%, #1a1a1a 100%);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(circle at 20% 80%, rgba(188, 82, 238, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(50, 69, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .hero-content {
    max-width: 800px;
    text-align: center;
    position: relative;
    z-index: 2;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    color: white;
    margin: 0 0 2rem 0;
    letter-spacing: -0.02em;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0 0 3rem 0;
    line-height: 1.6;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-logo-img {
    width: 96px;
    height: 96px;
    object-fit: contain;
    border-radius: 16px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.12);
    background: white;
    display: block;
    margin: 0 auto 2rem auto;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    margin-top: 4rem;
  }

  .stat-item {
    text-align: center;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    transition: transform 0.3s ease;
  }

  .stat-item:hover {
    transform: translateY(-5px);
  }

  .stat-label {
    display: block;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .stat-value {
    display: block;
    font-size: 3rem;
    font-weight: 700;
    color: white;
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 0 1rem;
      padding-top: 100px;
    }
    
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-subtitle {
      font-size: 1.1rem;
    }
    
    .stat-value {
      font-size: 2.5rem;
    }
    .hero-logo-img {
      width: 64px;
      height: 64px;
      margin-bottom: 1.2rem;
    }
  }
</style> 