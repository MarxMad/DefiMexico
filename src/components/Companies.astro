---
// Componente mejorado de Empresas DeFi México - Directorio de Startups
---

<section id="companies" class="companies-section">
  <div class="container">
    <div class="content-wrapper">
      
      <!-- Filters Sidebar -->
      <aside class="filters-sidebar">
        <h2 class="filters-title">Filtros</h2>
        <div class="filters-container">
          <div class="filter-group">
            <select class="filter-select" id="category-filter">
              <option value="">Categoría</option>
              <option value="exchange">Exchange</option>
              <option value="lending">Lending</option>
              <option value="wallet">Wallet</option>
              <option value="analytics">Analytics</option>
              <option value="defi">DeFi</option>
              <option value="payments">Payments</option>
            </select>
          </div>

          <div class="filter-group">
            <select class="filter-select" id="services-filter">
              <option value="">Servicios Ofrecidos</option>
              <option value="trading">Trading</option>
              <option value="lending">Lending</option>
              <option value="staking">Staking</option>
              <option value="yield">Yield Farming</option>
            </select>
          </div>

          <div class="filter-group">
            <select class="filter-select" id="funding-filter">
              <option value="">Fecha de Financiamiento</option>
              <option value="2024">2024</option>
              <option value="2023">2023</option>
              <option value="2022">2022</option>
              <option value="2021">2021</option>
            </select>
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="main-content">
        <div class="header-section">
          <div class="header-text">
            <h1 class="main-title">Startups DeFi en México</h1>
            <p class="main-subtitle">Descubre las startups más innovadoras del ecosistema cripto mexicano</p>
            <p class="results-count">Mostrando <span id="results-count">9</span> resultados</p>
          </div>
        </div>

        <!-- Startups Grid -->
        <div class="startups-grid" id="startups-grid">
          
          <!-- Crypto Exchange Mexico -->
          <div class="startup-card" data-category="exchange" data-services="trading" data-funding="2023">
            <div class="startup-logo bg-green-600">
              <span class="logo-text">CE</span>
            </div>
            <h3 class="startup-name">Crypto Exchange Mexico</h3>
            <p class="startup-description">
              Un exchange líder de criptomonedas en México, ofreciendo una plataforma segura para el trading de varios activos digitales.
            </p>
          </div>

          <!-- Lending Platform Mexico -->
          <div class="startup-card" data-category="lending" data-services="lending" data-funding="2023">
            <div class="startup-logo bg-green-700">
              <span class="logo-text">LP</span>
            </div>
            <h3 class="startup-name">Lending Platform Mexico</h3>
            <p class="startup-description">
              Una plataforma de préstamos descentralizada que conecta prestamistas y prestatarios en el mercado mexicano.
            </p>
          </div>

          <!-- DeFi Wallet Mexico -->
          <div class="startup-card" data-category="wallet" data-services="staking" data-funding="2022">
            <div class="startup-logo bg-green-800">
              <span class="logo-text">DW</span>
            </div>
            <h3 class="startup-name">DeFi Wallet Mexico</h3>
            <p class="startup-description">
              Una wallet no custodial diseñada para el ecosistema DeFi mexicano, proporcionando control total sobre los activos.
            </p>
          </div>

          <!-- Yield Farming Mexico -->
          <div class="startup-card" data-category="defi" data-services="yield" data-funding="2024">
            <div class="startup-logo bg-green-600">
              <span class="logo-text">YF</span>
            </div>
            <h3 class="startup-name">Yield Farming Mexico</h3>
            <p class="startup-description">
              Soluciones innovadoras de yield farming adaptadas para el mercado DeFi mexicano.
            </p>
          </div>

          <!-- DeFi Analytics Mexico -->
          <div class="startup-card" data-category="analytics" data-services="analytics" data-funding="2023">
            <div class="startup-logo bg-gray-100">
              <span class="logo-text text-gray-800">DA</span>
            </div>
            <h3 class="startup-name">DeFi Analytics Mexico</h3>
            <p class="startup-description">
              Plataforma integral de análisis para protocolos DeFi e inversiones en México.
            </p>
          </div>

          <!-- Stablecoin Mexico -->
          <div class="startup-card" data-category="defi" data-services="trading" data-funding="2022">
            <div class="startup-logo bg-green-700">
              <span class="logo-text">SM</span>
            </div>
            <h3 class="startup-name">Stablecoin Mexico</h3>
            <p class="startup-description">
              Stablecoin respaldada por peso mexicano para transacciones DeFi sin problemas.
            </p>
          </div>

          <!-- TacoSwap -->
          <div class="startup-card" data-category="exchange" data-services="trading" data-funding="2023">
            <div class="startup-logo bg-green-600">
              <span class="logo-text">🌮</span>
            </div>
            <h3 class="startup-name">TacoSwap</h3>
            <p class="startup-description">
              DEX nativo de México con enfoque en la comunidad latina y tokens locales.
            </p>
          </div>

          <!-- CactusPay -->
          <div class="startup-card" data-category="payments" data-services="trading" data-funding="2024">
            <div class="startup-logo bg-green-700">
              <span class="logo-text">🌵</span>
            </div>
            <h3 class="startup-name">CactusPay</h3>
            <p class="startup-description">
              Solución de pagos crypto para comercios mexicanos con integración POS.
            </p>
          </div>

          <!-- ChiliDAO -->
          <div class="startup-card" data-category="defi" data-services="staking" data-funding="2023">
            <div class="startup-logo bg-green-800">
              <span class="logo-text">🔥</span>
            </div>
            <h3 class="startup-name">ChiliDAO</h3>
            <p class="startup-description">
              Plataforma de governance descentralizada para startups del ecosistema mexicano.
            </p>
          </div>

        </div>
      </main>

    </div>
  </div>
</section>

<style>
  .companies-section {
    min-height: 100vh;
    background: #000000; /* Negro puro */
    padding: 4rem 1rem; /* Aumenté el padding vertical */
    display: flex;
    align-items: center; /* Centrar verticalmente */
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
  }

  .content-wrapper {
    display: flex;
    gap: 2rem;
    align-items: flex-start;
    width: 100%;
  }

  /* Filters Sidebar */
  .filters-sidebar {
    width: 20rem;
    flex-shrink: 0;
  }

  .filters-title {
    font-size: 2rem;
    font-weight: 700;
    color: white;
    margin: 0 0 1.5rem 0;
  }

  .filters-container {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .filter-group {
    position: relative;
  }

  .filter-select {
    width: 100%;
    padding: 0.75rem 1rem;
    background: #1e293b; /* slate-800 */
    border: 1px solid #475569; /* slate-700 */
    border-radius: 6px;
    color: white;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    appearance: none;
  }

  .filter-select:hover {
    background: #334155; /* slate-700 */
    border-color: #64748b; /* slate-500 */
  }

  .filter-select:focus {
    outline: none;
    border-color: #3b82f6; /* blue-500 */
    background: #334155;
  }

  .filter-select option {
    background: #1e293b;
    color: white;
  }

  /* Arrow for select */
  .filter-group::after {
    content: '▼';
    position: absolute;
    right: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: rgba(255, 255, 255, 0.7);
    pointer-events: none;
    font-size: 0.8rem;
  }

  /* Main Content */
  .main-content {
    flex: 1;
  }

  .header-section {
    margin-bottom: 3rem;
    text-align: center;
    width: 100%;
  }

  .header-text {
    max-width: 800px; /* Limitar ancho del texto */
    margin: 0 auto; /* Centrar horizontalmente */
  }

  .main-title {
    font-size: 4rem;
    font-weight: 700;
    color: white;
    margin: 0 0 0.5rem 0;
    letter-spacing: -0.02em;
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-align: center;
  }

  .main-subtitle {
    font-size: 1.25rem;
    color: #9ca3af; /* gray-400 */
    margin-bottom: 1.5rem;
    text-align: center;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .results-count {
    font-size: 1rem;
    color: #9ca3af; /* gray-400 */
    margin: 0;
  }

  /* Startups Grid */
  .startups-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .startup-card {
    background: #1e293b; /* slate-800 */
    border-radius: 0.5rem;
    padding: 1.5rem;
    transition: all 0.3s ease;
    cursor: pointer;
    border: 1px solid transparent;
  }

  .startup-card:hover {
    background: #334155; /* slate-700 */
    transform: translateY(-2px);
    border-color: rgba(255, 255, 255, 0.1);
  }

  .startup-logo {
    width: 4rem;
    height: 4rem;
    border-radius: 0.5rem;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.125rem;
  }

  /* Logo color classes */
  .bg-green-600 {
    background-color: #16a34a;
  }

  .bg-green-700 {
    background-color: #15803d;
  }

  .bg-green-800 {
    background-color: #166534;
  }

  .bg-gray-100 {
    background-color: #f3f4f6;
  }

  .logo-text {
    color: white;
  }

  .text-gray-800 {
    color: #1f2937 !important;
  }

  .startup-name {
    font-size: 1.25rem;
    font-weight: 600;
    color: white;
    margin: 0 0 0.75rem 0;
    line-height: 1.3;
  }

  .startup-description {
    font-size: 0.875rem;
    color: #9ca3af; /* gray-400 */
    line-height: 1.5;
    margin: 0;
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .content-wrapper {
      flex-direction: column;
    }
    
    .filters-sidebar {
      width: 100%;
    }
    
    .filters-container {
      flex-direction: row;
      flex-wrap: wrap;
    }
    
    .filter-group {
      flex: 1;
      min-width: 200px;
    }
    
    .main-title {
      font-size: 3rem;
    }
  }

  @media (max-width: 768px) {
    .companies-section {
      padding: 1rem 0.5rem;
    }
    
    .content-wrapper {
      gap: 1.5rem;
    }
    
    .filters-container {
      flex-direction: column;
    }
    
    .filter-group {
      min-width: auto;
    }
    
    .main-title {
      font-size: 2.5rem;
    }
    
    .startups-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    
    .startup-card {
      padding: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .main-title {
      font-size: 2rem;
    }
    
    .startup-card {
      padding: 1rem;
    }
    
    .startup-logo {
      width: 3rem;
      height: 3rem;
      font-size: 1rem;
    }
  }
</style>

<script>
  // Funcionalidad de filtrado
  document.addEventListener('DOMContentLoaded', function() {
    const categoryFilter = document.getElementById('category-filter');
    const servicesFilter = document.getElementById('services-filter');
    const fundingFilter = document.getElementById('funding-filter');
    const startupsGrid = document.getElementById('startups-grid');
    const resultsCount = document.getElementById('results-count');
    
    function filterStartups() {
      const categoryValue = categoryFilter.value;
      const servicesValue = servicesFilter.value;
      const fundingValue = fundingFilter.value;
      
      const allCards = startupsGrid.querySelectorAll('.startup-card');
      let visibleCount = 0;
      
      allCards.forEach(card => {
        const cardCategory = card.dataset.category;
        const cardServices = card.dataset.services;
        const cardFunding = card.dataset.funding;
        
        const categoryMatch = !categoryValue || cardCategory === categoryValue;
        const servicesMatch = !servicesValue || cardServices === servicesValue;
        const fundingMatch = !fundingValue || cardFunding === fundingValue;
        
        if (categoryMatch && servicesMatch && fundingMatch) {
          card.style.display = 'block';
          visibleCount++;
        } else {
          card.style.display = 'none';
        }
      });
      
      resultsCount.textContent = visibleCount;
    }
    
    // Event listeners para los filtros
    if (categoryFilter) categoryFilter.addEventListener('change', filterStartups);
    if (servicesFilter) servicesFilter.addEventListener('change', filterStartups);
    if (fundingFilter) fundingFilter.addEventListener('change', filterStartups);
    
    // Inicializar el conteo
    filterStartups();
  });
</script> 